#include <stdio.h>

int main() {
    int num1, num2, quotient;

printf("Enter first number: ");
scanf("%d", &num1);
printf("Enter second number: ");
scanf("%d", &num2);
quotient = num1 / num2;
printf("quotient = %d\n", quotient);

return 0;
}
